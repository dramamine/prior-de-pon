package gameplay
{
	public class Properties
	{
		// the time (in seconds) between a block matching up, and the block disappearing.
		public static const BLOCK_DISAPPEARING_DELAY:Number = 2.5;
		
		// the time (in seconds) that the screen will freeze after getting a combo/chain
		public static const SCREEN_FREEZE_LENGTH:Number = 5.5;
		
		
		public function Properties()
		{
		}
	}
}